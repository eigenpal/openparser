// This file is auto-generated by @hey-api/openapi-ts

export type ClientOptions = {
  baseUrl: 'https://{ocr_host}' | (string & {});
};

export type PublicFile = {
  id: string;
  filename: string;
  contentType: string | null;
  size: number | null;
  purpose: null;
  createdAt: string;
};

/**
 * Prefixed public id for an OCR job (`opj_…`).
 */
export type JobId = string;

/**
 * Stable hosted OCR model registry name from `GET /models`. Each catalog entry declares
 * its supported `ocr_options`, defaults, current availability, output capabilities, and
 * retail page price. Unknown model names return `422 unsupported_ocr_model`.
 *
 */
export type OcrModel = string;

/**
 * OpenRouter model slug from the compatible OCR extraction catalog
 * (`GET /models/llm`). Unknown or deprecated values return `422 unsupported_llm_model`.
 * Ordinary extract may use any currently compatible model; field grounding requires a
 * certified model.
 *
 */
export type LlmModel = string;

/**
 * Explicit schema-repair attempts allowed after a concrete invalid JSON/schema response.
 */
export type RepairAttempts = number;

/**
 * Supported JSON Schema object describing the requested extraction output.
 */
export type JsonSchemaObject = {
  [key: string]: unknown;
};

/**
 * Eigenpal reusable file-pool id from `POST /files`. Resolved with the same
 * tenant-scoped authorization as `GET /files/{id}/content`. Missing or
 * cross-tenant ids return `404 file_not_found`.
 *
 */
export type FileId = string;

export type ParseRequest = {
  ocr_model: string;
  ocr_options?: {
    [key: string]: unknown;
  };
  file_id?: string;
  output_format?: 'openparser@1' | 'raw';
};

/**
 * Single extract config. Provide either `pipeline_id` or inline extraction config.
 * The source is exactly one of multipart file bytes, `file_id`, or tenant-owned succeeded
 * `parse_job_id`. Parse reuse is single-extract-only, forbids `ocr_model` / `ocr_options`,
 * does not run OCR, and adds no page charge. `parse_job_id` is part of idempotency identity.
 *
 */
export type ExtractRequest = {
  /**
   * Saved extraction pipeline id (`oppl_…`). Inline extraction fields are forbidden.
   * Admission records pipeline id/version provenance and snapshots the effective extraction
   * configuration; the worker never re-reads the pipeline. With `parse_job_id`, source-parse
   * provenance is recorded separately and the pipeline's OCR settings are not executed.
   *
   */
  pipeline_id?: string;
  ocr_model?: string;
  ocr_options?: {
    [key: string]: unknown;
  };
  llm_model?: string;
  llm_options?: {
    reasoning_effort?: 'auto' | 'none' | 'minimal' | 'low' | 'medium' | 'high' | 'xhigh' | 'max';
  };
  schema?: {
    [key: string]: unknown;
  };
  repair_attempts?: number;
  /**
   * Optional field grounding mode. Defaults to `none` for inline requests. Changing
   * grounding with the same Idempotency-Key returns `409 idempotency_conflict`.
   * Unsupported schemas return `422 grounding_unsupported_schema`.
   *
   */
  grounding?: 'none' | 'field';
  file_id?: string;
  /**
   * Tenant-owned succeeded `parse` job (`opj_…`) whose canonical `openparser@1` ParsedDocument
   * is reused. Mutually exclusive with multipart bytes and `file_id`; forbidden in batch.
   *
   */
  parse_job_id?: string;
  output_format?: 'openparser@1' | 'raw';
};

/**
 * Suggest an extraction JSON Schema from a tenant-owned succeeded `parse` job.
 * Optional `hint` is caller guidance capped at 500 characters.
 *
 */
export type SuggestSchemaRequest = {
  /**
   * Prefixed id (`opj_…`) of a succeeded `parse` job in the same tenant.
   */
  parse_job_id: string;
  /**
   * Optional free-text guidance for the suggestion model (max 500 characters).
   */
  hint?: string;
};

/**
 * Create a tenant-scoped saved extraction configuration.
 */
export type CreateExtractionPipelineRequest = {
  name: string;
  slug?: string;
  ocr_model: string;
  ocr_options?: {
    [key: string]: unknown;
  };
  llm_model: string;
  llm_options?: {
    reasoning_effort?: 'auto' | 'none' | 'minimal' | 'low' | 'medium' | 'high' | 'xhigh' | 'max';
  };
  /**
   * JSON Schema object describing fields to extract. Root must be an object schema with
   * explicit `properties` and at least one named property. Admission applies the same
   * strict structured-output normalization as extract.
   *
   */
  schema: {
    [key: string]: unknown;
  };
  repair_attempts?: number;
  grounding?: 'none' | 'field';
};

/**
 * Partial update; at least one field required. Successful updates bump `version`.
 */
export type UpdateExtractionPipelineRequest = {
  name?: string;
  slug?: string | null;
  ocr_model?: string;
  ocr_options?: {
    [key: string]: unknown;
  } | null;
  llm_model?: string;
  llm_options?: {
    reasoning_effort?: 'auto' | 'none' | 'minimal' | 'low' | 'medium' | 'high' | 'xhigh' | 'max';
  } | null;
  /**
   * Replacement extraction schema. Root must be an object schema with explicit `properties`
   * and at least one named property. Merged pipeline config is re-validated on every update.
   *
   */
  schema?: {
    [key: string]: unknown;
  };
  repair_attempts?: number;
  grounding?: 'none' | 'field';
};

/**
 * Public saved extraction pipeline resource.
 */
export type ExtractionPipeline = {
  id: ExtractionPipelineId;
  name: string;
  slug: string | null;
  version: number;
  ocr_model: string;
  ocr_options: {
    [key: string]: unknown;
  };
  llm_model: LlmModel;
  llm_options: {
    reasoning_effort:
      | 'auto'
      | 'none'
      | 'minimal'
      | 'low'
      | 'medium'
      | 'high'
      | 'xhigh'
      | 'max'
      | null;
  };
  /**
   * Persisted extraction schema with at least one root property. Normalized for strict
   * structured outputs at save time.
   *
   */
  schema: {
    [key: string]: unknown;
  };
  repair_attempts: number;
  grounding: ExtractionGroundingMode;
  created_at: string;
  updated_at: string;
};

/**
 * Active pipelines for the authenticated tenant.
 */
export type ExtractionPipelineListResponse = {
  items: Array<ExtractionPipeline>;
};

/**
 * Soft-delete acknowledgement.
 */
export type DeleteExtractionPipelineResponse = {
  deleted: true;
  id: ExtractionPipelineId;
};

/**
 * Prefixed public id for a saved extraction pipeline (`oppl_…`).
 */
export type ExtractionPipelineId = string;

export type ParseBatchItem = {
  /**
   * Caller-supplied stable identifier for this batch item.
   */
  client_item_id: string;
  /**
   * Zero-based index into the uploaded `files` array. Exactly one of `file_index` or
   * `file_id` is required per item.
   *
   */
  file_index?: number;
  file_id?: string;
  ocr_model: string;
  ocr_options?: {
    [key: string]: unknown;
  };
};

/**
 * Per-item file-backed extract config with the same `pipeline_id` XOR inline rules as
 * single extract. Sources are `file_index` or `file_id`; `parse_job_id` is not supported
 * in batch. No parent-level batch pipeline in v1.
 *
 */
export type ExtractBatchItem = {
  client_item_id: string;
  /**
   * Zero-based index into the uploaded `files` array. Exactly one of `file_index` or
   * `file_id` is required per item.
   *
   */
  file_index?: number;
  file_id?: string;
  /**
   * Saved extraction pipeline id; mutually exclusive with inline extract fields.
   */
  pipeline_id?: string;
  ocr_model?: string;
  ocr_options?: {
    [key: string]: unknown;
  };
  llm_model?: string;
  llm_options?: {
    reasoning_effort?: 'auto' | 'none' | 'minimal' | 'low' | 'medium' | 'high' | 'xhigh' | 'max';
  };
  schema?: {
    [key: string]: unknown;
  };
  repair_attempts?: number;
  /**
   * Optional field grounding mode. Defaults to `none`. `field` opts into verified
   * per-leaf citations and is part of idempotency identity.
   *
   */
  grounding?: 'none' | 'field';
};

export type ParseBatchRequest = {
  /**
   * Ordered batch items. Each item references either an uploaded `files` entry via
   * `file_index` or an Eigenpal reusable `file_id`. Uploaded files must be covered by
   * exactly one unique `file_index` in `[0, files.length)`.
   *
   */
  items: Array<{
    client_item_id: string;
    file_index?: number;
    file_id?: string;
    ocr_model: string;
    ocr_options?: {
      [key: string]: unknown;
    };
  }>;
  output_format?: 'openparser@1' | 'raw';
};

export type ExtractBatchRequest = {
  /**
   * Ordered batch items. Each item references either an uploaded `files` entry via
   * `file_index` or an Eigenpal reusable `file_id`.
   *
   */
  items: Array<{
    client_item_id: string;
    file_index?: number;
    file_id?: string;
    pipeline_id?: string;
    ocr_model?: string;
    ocr_options?: {
      [key: string]: unknown;
    };
    llm_model?: string;
    llm_options?: {
      reasoning_effort?: 'auto' | 'none' | 'minimal' | 'low' | 'medium' | 'high' | 'xhigh' | 'max';
    };
    schema?: {
      [key: string]: unknown;
    };
    repair_attempts?: number;
    grounding?: 'none' | 'field';
  }>;
  output_format?: 'openparser@1' | 'raw';
};

export type OcrOutputFormat = 'openparser@1' | 'raw';

export type RawParseResult = {
  output_format: 'raw';
  provider: string;
  model: string;
  profile: {
    name: string;
    options: {
      [key: string]: unknown;
    };
  };
  result: {
    [key: string]: unknown;
  };
};

export type ParseResult = ParsedDocument | RawParseResult;

/**
 * Durable job status. For batch parent jobs, status is derived from children after each
 * child transition:
 * 1. If any child is non-terminal, the parent remains `queued` only while every child is
 * queued; it becomes `running` once any child is running or terminal.
 * 2. When every child is terminal and any child is `indeterminate`, the parent is
 * `indeterminate`.
 * 3. When every child is terminal, none are `indeterminate`, and any child is `failed`:
 * the parent is `failed` if zero children succeeded, otherwise `succeeded` (mixed
 * success/failure closes as parent `succeeded` while preserving failed children in
 * summaries).
 * 4. When every child is `succeeded`, the parent is `succeeded`.
 *
 * Partial failures are always retained on child summaries; parent status does not drop
 * failed children from results.
 *
 */
export type JobStatus = 'queued' | 'running' | 'succeeded' | 'failed' | 'indeterminate';

export type JobOperation = 'parse' | 'extract' | 'parse_batch' | 'extract_batch';

export type JobAccepted = {
  id: JobId;
  operation: 'parse' | 'extract';
  status: JobStatus;
  output_format: OcrOutputFormat;
  created_at: string;
  updated_at: string;
};

export type BatchJobAccepted = {
  id: JobId;
  operation: 'parse_batch' | 'extract_batch';
  status: JobStatus;
  output_format: OcrOutputFormat;
  created_at: string;
  updated_at: string;
  child_count: number;
};

export type Point = {
  x: number;
  y: number;
};

/**
 * Axis-aligned integer page coordinates with exclusive right and bottom edges.
 */
export type BoundingBox = {
  left: number;
  top: number;
  right: number;
  bottom: number;
};

export type Polygon = Array<Point>;

export type Geometry = {
  page_number: number;
  bbox: BoundingBox;
  polygon?: Polygon;
  rotation_degrees?: number;
};

export type Confidence = {
  score: number;
  scope: 'detection' | 'recognition' | 'classification' | 'geometry' | 'answer' | 'quality';
  calibrated: boolean;
  source_value?: number;
  source_scale?: 'zero_to_one' | 'zero_to_hundred' | 'log_probability' | 'unknown';
};

export type TextSpan = {
  start: number;
  end: number;
};

export type Language = {
  code: string;
  confidence?: Confidence;
};

export type TextStyle = {
  font_family?: string;
  font_size?: number;
  font_size_unit?: 'pixel' | 'point' | 'inch' | 'em' | 'unknown';
  font_weight?: number;
  bold?: boolean;
  italic?: boolean;
  underline?: boolean;
  strikethrough?: boolean;
  handwritten?: boolean;
  monospace?: boolean;
  small_caps?: boolean;
  superscript?: boolean;
  subscript?: boolean;
  foreground_color?: string;
  background_color?: string;
};

export type TextRole =
  | 'document_title'
  | 'heading'
  | 'paragraph'
  | 'line'
  | 'word'
  | 'symbol'
  | 'list'
  | 'list_item'
  | 'caption'
  | 'footnote'
  | 'page_header'
  | 'page_footer'
  | 'page_number'
  | 'code'
  | 'other';

export type SourceProvenance = {
  native_id?: string;
  native_type?: string;
  native_label?: string;
};

export type TableCell = {
  id: string;
  row_index: number;
  column_index: number;
  row_span: number;
  column_span: number;
  role: 'body' | 'column_header' | 'row_header' | 'stub' | 'title' | 'footer';
  text: string;
  spans: Array<TextSpan>;
  locations: Array<Geometry>;
  confidence?: Confidence;
  source?: SourceProvenance;
  element_ids: Array<string>;
};

export type StructuredValue = {
  text: string;
  spans: Array<TextSpan>;
  element_ids: Array<string>;
  locations: Array<Geometry>;
  confidence?: Confidence;
};

export type TextElement = {
  id: string;
  locations: Array<Geometry>;
  confidence?: Confidence;
  source?: SourceProvenance;
  kind: 'text';
  role: TextRole;
  text: string;
  spans: Array<TextSpan>;
  languages: Array<Language>;
  style?: TextStyle;
  break_after?: TextBreak;
};

export type TableElement = {
  id: string;
  locations: Array<Geometry>;
  confidence?: Confidence;
  source?: SourceProvenance;
  kind: 'table';
  row_count: number;
  column_count: number;
  cells: Array<TableCell>;
  html?: string;
  markdown?: string;
};

export type FigureElement = {
  id: string;
  locations: Array<Geometry>;
  confidence?: Confidence;
  source?: SourceProvenance;
  kind: 'figure';
  asset_id?: string;
  caption?: string;
  caption_spans: Array<TextSpan>;
  alt_text?: string;
};

export type FormulaElement = {
  id: string;
  locations: Array<Geometry>;
  confidence?: Confidence;
  source?: SourceProvenance;
  kind: 'formula';
  value: string;
  format: FormulaFormat;
  spans: Array<TextSpan>;
};

export type KeyValueElement = {
  id: string;
  locations: Array<Geometry>;
  confidence?: Confidence;
  source?: SourceProvenance;
  kind: 'key_value';
  key: StructuredValue;
  value: StructuredValue;
};

export type QueryAnswerElement = {
  id: string;
  locations: Array<Geometry>;
  confidence?: Confidence;
  source?: SourceProvenance;
  kind: 'query_answer';
  query: StructuredValue;
  answer: StructuredValue | null;
  alias?: string;
};

export type SectionElement = {
  id: string;
  locations: Array<Geometry>;
  confidence?: Confidence;
  source?: SourceProvenance;
  kind: 'section';
  role: SectionRole;
  title?: string;
  spans: Array<TextSpan>;
};

export type SelectionMarkElement = {
  id: string;
  locations: Array<Geometry>;
  confidence?: Confidence;
  source?: SourceProvenance;
  kind: 'selection_mark';
  state: SelectionState;
  mark_type: SelectionMarkType;
};

export type SignatureElement = {
  id: string;
  locations: Array<Geometry>;
  confidence?: Confidence;
  source?: SourceProvenance;
  kind: 'signature';
  text?: string;
};

export type BarcodeElement = {
  id: string;
  locations: Array<Geometry>;
  confidence?: Confidence;
  source?: SourceProvenance;
  kind: 'barcode';
  value: string;
  symbology?: string;
};

export type LinkElement = {
  id: string;
  locations: Array<Geometry>;
  confidence?: Confidence;
  source?: SourceProvenance;
  kind: 'link';
  url: string;
  text?: string;
  spans: Array<TextSpan>;
};

export type StampElement = {
  id: string;
  locations: Array<Geometry>;
  confidence?: Confidence;
  source?: SourceProvenance;
  kind: 'stamp';
  text?: string;
};

export type OtherElement = {
  id: string;
  locations: Array<Geometry>;
  confidence?: Confidence;
  source?: SourceProvenance;
  kind: 'other';
  label: string;
  text?: string;
};

export type TextBreak = 'none' | 'space' | 'wide_space' | 'hyphen' | 'line_break';

export type SectionRole = 'section' | 'chapter' | 'group' | 'other';

/**
 * One semantic node in the `openparser@1` document graph. Elements carry typed payloads
 * (text, table, figure, and other kinds), optional geometry in `locations`, and stable `id`
 * values referenced by pages, relations, assets, and grounding citations.
 *
 */
export type DocumentElement =
  | TextElement
  | TableElement
  | FigureElement
  | FormulaElement
  | KeyValueElement
  | QueryAnswerElement
  | SectionElement
  | SelectionMarkElement
  | SignatureElement
  | BarcodeElement
  | LinkElement
  | StampElement
  | OtherElement;

export type DocumentElementKind =
  | 'text'
  | 'table'
  | 'figure'
  | 'formula'
  | 'key_value'
  | 'query_answer'
  | 'section'
  | 'selection_mark'
  | 'signature'
  | 'barcode'
  | 'link'
  | 'stamp'
  | 'other';

export type TextAnnotation = {
  id: string;
  spans: Array<TextSpan>;
  languages: Array<Language>;
  style?: TextStyle;
  confidence?: Confidence;
  source?: SourceProvenance;
};

export type RelationType =
  | 'contains'
  | 'precedes'
  | 'continuation_of'
  | 'caption_of'
  | 'footnote_of'
  | 'refers_to'
  | 'overlaps';

export type DocumentRelation = {
  type: RelationType;
  from_id: string;
  to_id: string;
};

export type CoordinateUnit = 'pixel' | 'point' | 'inch' | 'normalized';

export type DocumentPage = {
  number: number;
  source_page_number?: number;
  width: number;
  height: number;
  unit: CoordinateUnit;
  rotation_degrees: number;
  languages: Array<Language>;
  confidence?: Confidence;
  quality?: {
    score?: Confidence;
    defects: Array<{
      type: string;
      confidence?: Confidence;
    }>;
    metrics: Array<{
      name: string;
      value: number | string | boolean;
    }>;
  };
  image_asset_id?: string;
  element_ids: Array<string>;
  reading_order: Array<string>;
};

export type AssetKind = 'page_image' | 'figure' | 'embedded_image' | 'other';

export type DocumentAsset = {
  id: string;
  kind: AssetKind;
  uri?: string;
  data_base64?: string;
  mime_type?: string;
  page_number?: number;
  width?: number;
  height?: number;
  sha256?: string;
};

export type DocumentProvenance = {
  provider: string;
  model: string;
  version?: string;
  operation?: string;
};

export type FormulaFormat = 'latex' | 'mathml' | 'plain' | 'unknown';

export type SelectionState = 'selected' | 'unselected' | 'indeterminate';

export type SelectionMarkType = 'checkbox' | 'radio' | 'other';

/**
 * Versioned OpenParser `openparser@1` document graph. `pages` establish coordinate spaces;
 * `elements` carry semantic payloads and geometry; `relations` preserve hierarchy and
 * cross-element meaning; `assets` hold reusable binary references (for example figure URIs).
 * Compatible optional fields may be added within version 1; breaking representation changes
 * create a new output-format version.
 *
 */
export type ParsedDocument = {
  output_format: 'openparser@1';
  document_id: string;
  provenance: DocumentProvenance;
  text: string;
  /**
   * Canonical best-effort Markdown rendering derived from the document graph.
   */
  markdown: string;
  /**
   * One-based contiguous page coordinate spaces with element membership and reading order.
   */
  pages: Array<DocumentPage>;
  /**
   * Ordered semantic nodes referenced by page membership, relations, and grounding citations.
   */
  elements: Array<DocumentElement>;
  text_annotations: Array<TextAnnotation>;
  relations: Array<DocumentRelation>;
  assets: Array<DocumentAsset>;
};

export type ExtractionAttemptKind = 'primary' | 'repair';

export type ExtractionAttemptStatus = 'succeeded' | 'failed' | 'indeterminate';

export type ExtractionAttempt = {
  /**
   * Zero-based attempt index in execution order.
   */
  index: number;
  kind: ExtractionAttemptKind;
  llm_model: LlmModel;
  status: ExtractionAttemptStatus;
  input_tokens?: number;
  output_tokens?: number;
  /**
   * Customer retail USD for this attempt (OpenRouter provider cost converted to the
   * rates customers are charged). Distinct from the OCR page charge.
   *
   */
  cost_usd?: number;
};

/**
 * Aggregated customer retail OpenRouter usage for transparency. `cost_usd` is the
 * customer charge, not raw provider list cost. Distinct from the customer
 * USD $0.001/page (0.1 credits/page) OCR charge recorded by Eigenpal billing.
 *
 */
export type ExtractionUsageTotals = {
  input_tokens?: number;
  output_tokens?: number;
  cost_usd?: number;
};

/**
 * Extraction grounding mode. `none` preserves ordinary extraction. `field` requests
 * verified per-leaf source citations (citation v1 emits element-level citations only).
 *
 */
export type ExtractionGroundingMode = 'none' | 'field';

/**
 * Citation geometry granularity. Citation v1 emits `element` and `table_cell`;
 * `text_span` is reserved for a later narrowing release.
 *
 */
export type ExtractionCitationGranularity = 'element' | 'table_cell' | 'text_span';

/**
 * Verified provenance for one extracted leaf citation. Geometry comes from the parse graph element or table cell, never the model.
 */
export type ExtractionCitation = {
  element_id: string;
  table_cell_id?: string;
  page_number: number;
  bbox: BoundingBox;
  polygon?: Polygon;
  source_type: DocumentElementKind;
  granularity: ExtractionCitationGranularity;
  confidence?: Confidence;
};

/**
 * Grounding metadata for one leaf in the unwrapped extraction output. `path` uses
 * dot-separated property segments and decimal array-index segments, for example
 * `line_items.0.amount`.
 *
 */
export type ExtractionGroundingField = {
  /**
   * Dot-separated path from the output root. Array positions are decimal segments.
   * Property names containing dots are outside the field-grounding schema subset.
   *
   */
  path: string;
  citations: Array<ExtractionCitation>;
  dropped_source_ids?: Array<string>;
};

/**
 * Optional terminal grounding envelope present only when `grounding: field` succeeded.
 */
export type ExtractionGroundingResult = {
  mode: 'field';
  fields: Array<ExtractionGroundingField>;
};

/**
 * Terminal extraction result returned by sync `200` and embedded in succeeded jobs.
 */
export type ExtractionTerminalResult = {
  /**
   * Validated extraction output JSON matching the requested schema (wrappers never leak).
   */
  output: unknown;
  parsed_document: ParsedDocument;
  llm_model: LlmModel;
  /**
   * Present only when extraction reused this source parse job without running OCR again.
   */
  parse_job_id?: JobId;
  reasoning_effort?: 'none' | 'minimal' | 'low' | 'medium' | 'high' | 'xhigh' | 'max' | null;
  attempts: Array<ExtractionAttempt>;
  usage?: ExtractionUsageTotals;
  /**
   * Present only for successful `grounding: field` jobs. Omitted for ordinary extraction.
   *
   */
  grounding?: ExtractionGroundingResult;
};

/**
 * Suggested extraction schema returned directly without creating a durable job.
 */
export type SuggestSchemaResponse = {
  /**
   * Short document-type label proposed by the model.
   */
  name: string;
  /**
   * Canonical JSON Schema object ready for extract and manual editing.
   */
  schema: {
    [key: string]: unknown;
  };
};

export type JobFailure = {
  code: string;
  message: string;
  retryable: boolean;
  details?: {
    [key: string]: unknown;
  };
};

export type BatchChildSummary = {
  client_item_id: string;
  job_id: JobId;
  file_index: number;
  status: JobStatus;
  output_format: OcrOutputFormat;
  error?: JobFailure;
  /**
   * Terminal child result when `status` is `succeeded`.
   */
  result?: ParsedDocument | RawParseResult | ExtractionTerminalResult;
};

export type BatchChildPage = {
  items: Array<BatchChildSummary>;
  /**
   * Present when more child summaries are available.
   */
  next_cursor?: string | null;
};

export type BatchSummaryCounts = {
  total: number;
  queued: number;
  running: number;
  succeeded: number;
  failed: number;
  indeterminate: number;
};

export type Job = {
  id: JobId;
  operation: JobOperation;
  status: JobStatus;
  output_format: OcrOutputFormat;
  created_at: string;
  updated_at: string;
  progress?: {
    completed?: number;
    total?: number;
  };
  error?: JobFailure;
  /**
   * Terminal result for single-item parse or extract jobs.
   */
  result?: ParsedDocument | RawParseResult | ExtractionTerminalResult;
  summary?: BatchSummaryCounts;
  children?: BatchChildPage;
  has_source?: boolean;
  source_media_type?: string | null;
  source_parse_job_id?: JobId | null;
  related_extractions?: Array<{
    id: JobId;
    status: JobStatus;
    created_at: string;
    updated_at: string;
    llm_model?: string | null;
    has_result: boolean;
    url: string;
  }>;
  extraction_schema?: {
    [key: string]: unknown;
  };
  /**
   * OCR model used at admit time when recorded. Omitted when unknown. Jobs do not
   * expose a durable OCR options snapshot.
   *
   */
  ocr_model?: string | null;
  /**
   * Snapshot of the saved extraction pipeline id at admit time, or `null` for parse jobs,
   * inline extract config, and other non-pipeline work.
   *
   */
  pipeline_id: ExtractionPipelineId | null;
  /**
   * Snapshot of the saved extraction pipeline version at admit time, or `null` when
   * `pipeline_id` is `null`.
   *
   */
  pipeline_version: number | null;
};

/**
 * Lightweight list row for `GET /jobs`. Omits result bodies, idempotency keys,
 * storage object keys, and provider cost fields.
 *
 */
export type JobSummary = {
  id: JobId;
  operation: JobOperation;
  status: JobStatus;
  output_format: OcrOutputFormat;
  ocr_model?: string | null;
  llm_model?: string | null;
  page_count: number;
  created_at: string;
  updated_at: string;
  error?: JobFailure;
  /**
   * True when `GET /jobs/{id}` includes a terminal `result`.
   */
  has_result: boolean;
  /**
   * Path-absolute job resource URL (for example `/jobs/{id}`).
   */
  url: string;
  has_source?: boolean;
  /**
   * Snapshot of the saved extraction pipeline id at admit time, or `null` for parse jobs,
   * inline extract config, and other non-pipeline work.
   *
   */
  pipeline_id: ExtractionPipelineId | null;
  /**
   * Snapshot of the saved extraction pipeline version at admit time, or `null` when
   * `pipeline_id` is `null`.
   *
   */
  pipeline_version: number | null;
};

/**
 * Cursor-paginated page of job summaries for the authenticated tenant.
 */
export type JobListResponse = {
  data: Array<JobSummary>;
  /**
   * Opaque cursor for the next page, or `null` when exhausted.
   */
  next_cursor: string | null;
};

export type ErrorBody = {
  code: string;
  message: string;
  request_id: string;
  retryable: boolean;
  details?: {
    [key: string]: unknown;
  };
};

export type ErrorResponse = {
  error: ErrorBody;
};

export type DeleteFileResponse = {
  deleted: true;
};

/**
 * Public OCR model registry entry. `pricing.usd_per_page` is the
 * customer retail page price at option defaults (`basis: customer_retail`).
 * Optional `pricing.configurations` lists exact named retail totals for
 * price-affecting option combinations — never additive surcharges.
 */
export type OcrModelCatalogEntry = {
  id: string;
  label: string;
  is_default: boolean;
  provider: {
    key: string;
    label: string;
    logo?: string;
    logo_data?: {
      title: string;
      view_box: string;
      paths: Array<{
        d: string;
        clip_rule?: 'evenodd' | 'nonzero';
        fill_opacity?: string;
      }>;
    };
  };
  guidance: {
    summary: string;
    best_for: string;
    trade_off: string;
    output: string;
  };
  benchmark: {
    score: number;
    version: string;
    qualification: string;
    source_url: string;
  } | null;
  output_summary: string;
  capabilities: {
    parse: boolean;
    extract_source: boolean;
    markdown: boolean;
    regions: boolean;
    options: {
      [key: string]: boolean;
    };
  };
  option_defaults: {
    [key: string]: unknown;
  };
  option_controls: Array<{
    key: string;
    label: string;
    summary: string;
    kind: 'boolean' | 'enum' | 'nullable-enum' | 'nullable-string' | 'nullable-string-list';
    choices?: Array<{
      value: string;
      label: string;
    }>;
    placeholder?: string;
    nullable_sentinel?: string;
  }>;
  pricing: {
    usd_per_page: number;
    basis: 'customer_retail';
    configurations?: Array<{
      label: string;
      usd_per_page: number;
      options: {
        [key: string]: boolean | 'set' | 'unset';
      };
    }>;
  };
  availability: 'available' | 'degraded' | 'unavailable';
};

/**
 * OCR model discovery response for `GET /models/ocr`.
 */
export type OcrModelsResponse = {
  data: Array<OcrModelCatalogEntry>;
};

/**
 * One compatible extraction LLM. `recommendation` is `suggested` or `compatible`.
 * `certified_grounding` / `certified_suggest` (and compatibility aliases
 * `supports_grounding` / `supports_suggest`) gate heavier schemas. `pricing` is customer retail USD per 1M tokens
 * (`basis: customer_retail`) — the rates customers are charged, not raw provider list cost.
 *
 */
export type OcrLlmModelCatalogEntry = {
  id: string;
  label: string;
  provider: string;
  created_at: string | null;
  context_length: number | null;
  recommendation: 'suggested' | 'compatible';
  is_default: boolean;
  certified_grounding: boolean;
  certified_suggest: boolean;
  supports_grounding: boolean;
  supports_suggest: boolean;
  pricing_known: true;
  reasoning: {
    supported_efforts: Array<
      'none' | 'minimal' | 'low' | 'medium' | 'high' | 'xhigh' | 'max'
    > | null;
    default_effort: 'none' | 'minimal' | 'low' | 'medium' | 'high' | 'xhigh' | 'max' | null;
    mandatory: boolean;
    supports_max_tokens: boolean;
  } | null;
  deprecated_at: string | null;
  pricing: {
    prompt_usd_per_1m: number;
    completion_usd_per_1m: number;
    basis: 'customer_retail';
  };
};

/**
 * Normalized OpenRouter LLM catalog for OpenParser extraction. Includes catalog version,
 * stale flag, and pagination metadata for search mode.
 *
 */
export type OcrLlmModelsResponse = {
  mode: 'suggested' | 'search';
  catalog_version: string;
  fetched_at: string;
  stale: boolean;
  data: Array<OcrLlmModelCatalogEntry>;
  page: number;
  limit: number;
  total: number;
  has_more: boolean;
};

/**
 * Client-supplied idempotency key. Required on every parse or extract `POST`. Must be unique per distinct
 * request body for a given tenant and operation.
 *
 */
export type IdempotencyKey = string;

/**
 * Durable OCR job identifier (`opj_…`).
 */
export type JobId2 = JobId;

/**
 * Tenant-scoped reusable file identifier.
 */
export type FileId2 = string;

/**
 * Tenant-scoped saved extraction pipeline identifier (`oppl_…`).
 */
export type PipelineId = ExtractionPipelineId;

/**
 * Opaque pagination cursor for batch child summaries.
 */
export type ChildCursor = string;

/**
 * Maximum number of batch child summaries to return.
 */
export type ChildLimit = number;

/**
 * Opaque pagination cursor from a prior `JobListResponse.next_cursor`. Jobs are
 * returned newest-first (`created_at` descending, `id` descending tie-break).
 *
 */
export type JobListCursor = string;

/**
 * Maximum number of job summaries to return.
 */
export type JobListLimit = number;

/**
 * Optional filter by public job status. Admission-phase (`admitting`) jobs are
 * never listed.
 *
 */
export type JobListStatus = JobStatus;

/**
 * Optional filter by job operation.
 */
export type JobListOperation = JobOperation;

/**
 * Parse representation to return. Defaults to the job's original `output_format`
 * when omitted. Fresh parses store both representations as immutable object-store
 * artifacts; historical jobs only expose the representation already present in
 * `result_json`. Requesting an unavailable alternate returns `422` and never
 * re-runs OCR.
 *
 */
export type ParseResultFormat = OcrOutputFormat;

/**
 * `suggested` (default): short recommended subset.
 * `search`: full compatible catalog filtered by `q`.
 *
 */
export type LlmModelsMode = 'suggested' | 'search';

/**
 * Case-insensitive substring match against id, label, or provider. Used with `mode=search`.
 */
export type LlmModelsQuery = string;

/**
 * 1-based page index when paginating search results.
 */
export type LlmModelsPage = number;

/**
 * Page size for search; ignored for suggested mode (bounded server-side).
 */
export type LlmModelsLimit = number;

export type ParseSingleUpload = {
  /**
   * Source document bytes (PDF, PNG, or JPEG). Required unless `request.file_id`
   * references an existing Eigenpal reusable file.
   *
   */
  file?: Blob | File;
  request: ParseRequest;
};

export type ExtractSingleUpload = {
  /**
   * Source document bytes (PDF, PNG, or JPEG). Required unless `request.file_id`
   * references an existing Eigenpal reusable file.
   *
   */
  file?: Blob | File;
  request: ExtractRequest;
};

export type ParseBatchUpload = {
  /**
   * Ordered uploaded files referenced by `file_index` in the batch request. Each file
   * is limited to 50 MiB and total multipart body content is limited to 100 MiB.
   * Omit when every batch item supplies `file_id` instead.
   *
   */
  files?: Array<Blob | File>;
  request: ParseBatchRequest;
};

export type ExtractBatchUpload = {
  /**
   * Ordered uploaded files referenced by `file_index` in the batch request. Each file
   * is limited to 50 MiB and total multipart body content is limited to 100 MiB.
   * Omit when every batch item supplies `file_id` instead.
   *
   */
  files?: Array<Blob | File>;
  request: ExtractBatchRequest;
};

export type SuggestSchemaRequest2 = SuggestSchemaRequest;

export type CreateExtractionPipelineRequest2 = CreateExtractionPipelineRequest;

export type UpdateExtractionPipelineRequest2 = UpdateExtractionPipelineRequest;

export type CreateFileUpload = {
  file: Blob | File;
};

export type ListOcrModelsData = {
  body?: never;
  path?: never;
  query?: never;
  url: '/models/ocr';
};

export type ListOcrModelsErrors = {
  /**
   * Missing or invalid API key
   */
  401: ErrorResponse;
  /**
   * API key lacks required OCR scope (`*` or `ocr:full`)
   */
  403: ErrorResponse;
  /**
   * Rate or tenant concurrency limit exceeded. Tenant capacity uses
   * `tenant_limit_exceeded` when the caller already holds the configured maximum
   * of executable non-terminal jobs (`admitting`/`queued`/`running` singles and
   * batch children; batch parents do not count). Idempotent replays of an
   * existing equivalent job are still admitted.
   *
   */
  429: ErrorResponse;
};

export type ListOcrModelsError = ListOcrModelsErrors[keyof ListOcrModelsErrors];

export type ListOcrModelsResponses = {
  /**
   * Successful response
   */
  200: OcrModelsResponse;
};

export type ListOcrModelsResponse = ListOcrModelsResponses[keyof ListOcrModelsResponses];

export type ListLlmModelsData = {
  body?: never;
  path?: never;
  query?: {
    /**
     * `suggested` (default): short recommended subset.
     * `search`: full compatible catalog filtered by `q`.
     *
     */
    mode?: 'suggested' | 'search';
    /**
     * Case-insensitive substring match against id, label, or provider. Used with `mode=search`.
     */
    q?: string;
    /**
     * 1-based page index when paginating search results.
     */
    page?: number;
    /**
     * Page size for search; ignored for suggested mode (bounded server-side).
     */
    limit?: number;
  };
  url: '/models/llm';
};

export type ListLlmModelsErrors = {
  /**
   * Missing or invalid API key
   */
  401: ErrorResponse;
  /**
   * API key lacks required OCR scope (`*` or `ocr:full`)
   */
  403: ErrorResponse;
  /**
   * Rate or tenant concurrency limit exceeded. Tenant capacity uses
   * `tenant_limit_exceeded` when the caller already holds the configured maximum
   * of executable non-terminal jobs (`admitting`/`queued`/`running` singles and
   * batch children; batch parents do not count). Idempotent replays of an
   * existing equivalent job are still admitted.
   *
   */
  429: ErrorResponse;
};

export type ListLlmModelsError = ListLlmModelsErrors[keyof ListLlmModelsErrors];

export type ListLlmModelsResponses = {
  /**
   * Successful response
   */
  200: OcrLlmModelsResponse;
};

export type ListLlmModelsResponse = ListLlmModelsResponses[keyof ListLlmModelsResponses];

export type ParseSyncData = {
  body: ParseSingleUpload;
  headers: {
    /**
     * Client-supplied idempotency key. Required on every parse or extract `POST`. Must be unique per distinct
     * request body for a given tenant and operation.
     *
     */
    'Idempotency-Key': string;
  };
  path?: never;
  query?: never;
  url: '/parse';
};

export type ParseSyncErrors = {
  /**
   * Malformed request
   */
  400: ErrorResponse;
  /**
   * Missing or invalid API key
   */
  401: ErrorResponse;
  /**
   * Billing or credits check failed
   */
  402: ErrorResponse;
  /**
   * API key lacks required OCR scope (`*` or `ocr:full`)
   */
  403: ErrorResponse;
  /**
   * Idempotency key reused with a different request
   */
  409: ErrorResponse;
  /**
   * Document or request limits exceeded
   */
  413: ErrorResponse;
  /**
   * Unsupported uploaded media type
   */
  415: ErrorResponse;
  /**
   * Admission-time unprocessable configuration/schema/media policy rejection, or (sync
   * `POST /parse` and `POST /extract` only) a durable job that reached terminal
   * `failed` within the sync wait window. Body is always `ErrorResponse`. Async and batch
   * admissions use `UnprocessableConfig` semantics only; their terminal failures are read
   * via `GET /jobs/{id}`.
   *
   */
  422: ErrorResponse;
  /**
   * Rate or tenant concurrency limit exceeded. Tenant capacity uses
   * `tenant_limit_exceeded` when the caller already holds the configured maximum
   * of executable non-terminal jobs (`admitting`/`queued`/`running` singles and
   * batch children; batch parents do not count). Idempotent replays of an
   * existing equivalent job are still admitted.
   *
   */
  429: ErrorResponse;
  /**
   * API admission dependency failure (for example OCR source object upload to dedicated
   * storage). Transient GPU/HPS unavailability is **not** reported as this response —
   * known registry models remain admissible and jobs stay durable/queued; tenant caps and
   * queue alarms bound/observe backlog.
   *
   */
  503: ErrorResponse;
  /**
   * Sync wait observed a durable job terminal status of `indeterminate` (for example an
   * ambiguous provider acceptance). Body is `ErrorResponse`. Clients should not assume the
   * operation can be safely retried without consulting job and billing state.
   *
   */
  504: ErrorResponse;
};

export type ParseSyncError = ParseSyncErrors[keyof ParseSyncErrors];

export type ParseSyncResponses = {
  /**
   * Terminal parse result in the selected `openparser@1` or `raw` representation
   */
  200: ParseResult;
  /**
   * Durable non-terminal job admitted. Returned by async endpoints and by sync endpoints when
   * the wait limit expires before a terminal status.
   *
   */
  202: JobAccepted;
};

export type ParseSyncResponse = ParseSyncResponses[keyof ParseSyncResponses];

export type ParseAsyncData = {
  body: ParseSingleUpload;
  headers: {
    /**
     * Client-supplied idempotency key. Required on every parse or extract `POST`. Must be unique per distinct
     * request body for a given tenant and operation.
     *
     */
    'Idempotency-Key': string;
  };
  path?: never;
  query?: never;
  url: '/parse/async';
};

export type ParseAsyncErrors = {
  /**
   * Malformed request
   */
  400: ErrorResponse;
  /**
   * Missing or invalid API key
   */
  401: ErrorResponse;
  /**
   * Billing or credits check failed
   */
  402: ErrorResponse;
  /**
   * API key lacks required OCR scope (`*` or `ocr:full`)
   */
  403: ErrorResponse;
  /**
   * Idempotency key reused with a different request
   */
  409: ErrorResponse;
  /**
   * Document or request limits exceeded
   */
  413: ErrorResponse;
  /**
   * Unsupported uploaded media type
   */
  415: ErrorResponse;
  /**
   * Invalid schema, configuration, encrypted PDF, unsupported OCR model, or unsupported LLM model
   */
  422: ErrorResponse;
  /**
   * Rate or tenant concurrency limit exceeded. Tenant capacity uses
   * `tenant_limit_exceeded` when the caller already holds the configured maximum
   * of executable non-terminal jobs (`admitting`/`queued`/`running` singles and
   * batch children; batch parents do not count). Idempotent replays of an
   * existing equivalent job are still admitted.
   *
   */
  429: ErrorResponse;
  /**
   * API admission dependency failure (for example OCR source object upload to dedicated
   * storage). Transient GPU/HPS unavailability is **not** reported as this response —
   * known registry models remain admissible and jobs stay durable/queued; tenant caps and
   * queue alarms bound/observe backlog.
   *
   */
  503: ErrorResponse;
};

export type ParseAsyncError = ParseAsyncErrors[keyof ParseAsyncErrors];

export type ParseAsyncResponses = {
  /**
   * Durable non-terminal job admitted. Returned by async endpoints and by sync endpoints when
   * the wait limit expires before a terminal status.
   *
   */
  202: JobAccepted;
};

export type ParseAsyncResponse = ParseAsyncResponses[keyof ParseAsyncResponses];

export type ParseBatchData = {
  body: ParseBatchUpload;
  headers: {
    /**
     * Client-supplied idempotency key. Required on every parse or extract `POST`. Must be unique per distinct
     * request body for a given tenant and operation.
     *
     */
    'Idempotency-Key': string;
  };
  path?: never;
  query?: never;
  url: '/parse/batch';
};

export type ParseBatchErrors = {
  /**
   * Malformed request
   */
  400: ErrorResponse;
  /**
   * Missing or invalid API key
   */
  401: ErrorResponse;
  /**
   * Billing or credits check failed
   */
  402: ErrorResponse;
  /**
   * API key lacks required OCR scope (`*` or `ocr:full`)
   */
  403: ErrorResponse;
  /**
   * Idempotency key reused with a different request
   */
  409: ErrorResponse;
  /**
   * Document or request limits exceeded
   */
  413: ErrorResponse;
  /**
   * Unsupported uploaded media type
   */
  415: ErrorResponse;
  /**
   * Invalid schema, configuration, encrypted PDF, unsupported OCR model, or unsupported LLM model
   */
  422: ErrorResponse;
  /**
   * Rate or tenant concurrency limit exceeded. Tenant capacity uses
   * `tenant_limit_exceeded` when the caller already holds the configured maximum
   * of executable non-terminal jobs (`admitting`/`queued`/`running` singles and
   * batch children; batch parents do not count). Idempotent replays of an
   * existing equivalent job are still admitted.
   *
   */
  429: ErrorResponse;
  /**
   * API admission dependency failure (for example OCR source object upload to dedicated
   * storage). Transient GPU/HPS unavailability is **not** reported as this response —
   * known registry models remain admissible and jobs stay durable/queued; tenant caps and
   * queue alarms bound/observe backlog.
   *
   */
  503: ErrorResponse;
};

export type ParseBatchError = ParseBatchErrors[keyof ParseBatchErrors];

export type ParseBatchResponses = {
  /**
   * Durable batch job admitted. `Location` points at the parent batch job resource.
   *
   */
  202: BatchJobAccepted;
};

export type ParseBatchResponse = ParseBatchResponses[keyof ParseBatchResponses];

export type ExtractSyncData = {
  body: ExtractSingleUpload;
  headers: {
    /**
     * Client-supplied idempotency key. Required on every parse or extract `POST`. Must be unique per distinct
     * request body for a given tenant and operation.
     *
     */
    'Idempotency-Key': string;
  };
  path?: never;
  query?: never;
  url: '/extract';
};

export type ExtractSyncErrors = {
  /**
   * Malformed request
   */
  400: ErrorResponse;
  /**
   * Missing or invalid API key
   */
  401: ErrorResponse;
  /**
   * Billing or credits check failed
   */
  402: ErrorResponse;
  /**
   * API key lacks required OCR scope (`*` or `ocr:full`)
   */
  403: ErrorResponse;
  /**
   * Idempotency key reused with a different request
   */
  409: ErrorResponse;
  /**
   * Document or request limits exceeded
   */
  413: ErrorResponse;
  /**
   * Unsupported uploaded media type
   */
  415: ErrorResponse;
  /**
   * Admission-time unprocessable configuration/schema/media policy rejection, or (sync
   * `POST /parse` and `POST /extract` only) a durable job that reached terminal
   * `failed` within the sync wait window. Body is always `ErrorResponse`. Async and batch
   * admissions use `UnprocessableConfig` semantics only; their terminal failures are read
   * via `GET /jobs/{id}`.
   *
   */
  422: ErrorResponse;
  /**
   * Rate or tenant concurrency limit exceeded. Tenant capacity uses
   * `tenant_limit_exceeded` when the caller already holds the configured maximum
   * of executable non-terminal jobs (`admitting`/`queued`/`running` singles and
   * batch children; batch parents do not count). Idempotent replays of an
   * existing equivalent job are still admitted.
   *
   */
  429: ErrorResponse;
  /**
   * API admission dependency failure (for example OCR source object upload to dedicated
   * storage). Transient GPU/HPS unavailability is **not** reported as this response —
   * known registry models remain admissible and jobs stay durable/queued; tenant caps and
   * queue alarms bound/observe backlog.
   *
   */
  503: ErrorResponse;
  /**
   * Sync wait observed a durable job terminal status of `indeterminate` (for example an
   * ambiguous provider acceptance). Body is `ErrorResponse`. Clients should not assume the
   * operation can be safely retried without consulting job and billing state.
   *
   */
  504: ErrorResponse;
};

export type ExtractSyncError = ExtractSyncErrors[keyof ExtractSyncErrors];

export type ExtractSyncResponses = {
  /**
   * Terminal extraction result
   */
  200: ExtractionTerminalResult;
  /**
   * Durable non-terminal job admitted. Returned by async endpoints and by sync endpoints when
   * the wait limit expires before a terminal status.
   *
   */
  202: JobAccepted;
};

export type ExtractSyncResponse = ExtractSyncResponses[keyof ExtractSyncResponses];

export type ExtractAsyncData = {
  body: ExtractSingleUpload;
  headers: {
    /**
     * Client-supplied idempotency key. Required on every parse or extract `POST`. Must be unique per distinct
     * request body for a given tenant and operation.
     *
     */
    'Idempotency-Key': string;
  };
  path?: never;
  query?: never;
  url: '/extract/async';
};

export type ExtractAsyncErrors = {
  /**
   * Malformed request
   */
  400: ErrorResponse;
  /**
   * Missing or invalid API key
   */
  401: ErrorResponse;
  /**
   * Billing or credits check failed
   */
  402: ErrorResponse;
  /**
   * API key lacks required OCR scope (`*` or `ocr:full`)
   */
  403: ErrorResponse;
  /**
   * Idempotency key reused with a different request
   */
  409: ErrorResponse;
  /**
   * Document or request limits exceeded
   */
  413: ErrorResponse;
  /**
   * Unsupported uploaded media type
   */
  415: ErrorResponse;
  /**
   * Invalid schema, configuration, encrypted PDF, unsupported OCR model, or unsupported LLM model
   */
  422: ErrorResponse;
  /**
   * Rate or tenant concurrency limit exceeded. Tenant capacity uses
   * `tenant_limit_exceeded` when the caller already holds the configured maximum
   * of executable non-terminal jobs (`admitting`/`queued`/`running` singles and
   * batch children; batch parents do not count). Idempotent replays of an
   * existing equivalent job are still admitted.
   *
   */
  429: ErrorResponse;
  /**
   * API admission dependency failure (for example OCR source object upload to dedicated
   * storage). Transient GPU/HPS unavailability is **not** reported as this response —
   * known registry models remain admissible and jobs stay durable/queued; tenant caps and
   * queue alarms bound/observe backlog.
   *
   */
  503: ErrorResponse;
};

export type ExtractAsyncError = ExtractAsyncErrors[keyof ExtractAsyncErrors];

export type ExtractAsyncResponses = {
  /**
   * Durable non-terminal job admitted. Returned by async endpoints and by sync endpoints when
   * the wait limit expires before a terminal status.
   *
   */
  202: JobAccepted;
};

export type ExtractAsyncResponse = ExtractAsyncResponses[keyof ExtractAsyncResponses];

export type ExtractBatchData = {
  body: ExtractBatchUpload;
  headers: {
    /**
     * Client-supplied idempotency key. Required on every parse or extract `POST`. Must be unique per distinct
     * request body for a given tenant and operation.
     *
     */
    'Idempotency-Key': string;
  };
  path?: never;
  query?: never;
  url: '/extract/batch';
};

export type ExtractBatchErrors = {
  /**
   * Malformed request
   */
  400: ErrorResponse;
  /**
   * Missing or invalid API key
   */
  401: ErrorResponse;
  /**
   * Billing or credits check failed
   */
  402: ErrorResponse;
  /**
   * API key lacks required OCR scope (`*` or `ocr:full`)
   */
  403: ErrorResponse;
  /**
   * Idempotency key reused with a different request
   */
  409: ErrorResponse;
  /**
   * Document or request limits exceeded
   */
  413: ErrorResponse;
  /**
   * Unsupported uploaded media type
   */
  415: ErrorResponse;
  /**
   * Invalid schema, configuration, encrypted PDF, unsupported OCR model, or unsupported LLM model
   */
  422: ErrorResponse;
  /**
   * Rate or tenant concurrency limit exceeded. Tenant capacity uses
   * `tenant_limit_exceeded` when the caller already holds the configured maximum
   * of executable non-terminal jobs (`admitting`/`queued`/`running` singles and
   * batch children; batch parents do not count). Idempotent replays of an
   * existing equivalent job are still admitted.
   *
   */
  429: ErrorResponse;
  /**
   * API admission dependency failure (for example OCR source object upload to dedicated
   * storage). Transient GPU/HPS unavailability is **not** reported as this response —
   * known registry models remain admissible and jobs stay durable/queued; tenant caps and
   * queue alarms bound/observe backlog.
   *
   */
  503: ErrorResponse;
};

export type ExtractBatchError = ExtractBatchErrors[keyof ExtractBatchErrors];

export type ExtractBatchResponses = {
  /**
   * Durable batch job admitted. `Location` points at the parent batch job resource.
   *
   */
  202: BatchJobAccepted;
};

export type ExtractBatchResponse = ExtractBatchResponses[keyof ExtractBatchResponses];

export type SuggestSchemaData = {
  body: SuggestSchemaRequest2;
  path?: never;
  query?: never;
  url: '/suggest-schema';
};

export type SuggestSchemaErrors = {
  /**
   * Malformed request
   */
  400: ErrorResponse;
  /**
   * Missing or invalid API key
   */
  401: ErrorResponse;
  /**
   * API key lacks required OCR scope (`*` or `ocr:full`)
   */
  403: ErrorResponse;
  /**
   * Job not found for the authenticated tenant
   */
  404: ErrorResponse;
  /**
   * Admission-time unprocessable configuration/schema/media policy rejection, or (sync
   * `POST /parse` and `POST /extract` only) a durable job that reached terminal
   * `failed` within the sync wait window. Body is always `ErrorResponse`. Async and batch
   * admissions use `UnprocessableConfig` semantics only; their terminal failures are read
   * via `GET /jobs/{id}`.
   *
   */
  422: ErrorResponse;
  /**
   * Rate or tenant concurrency limit exceeded. Tenant capacity uses
   * `tenant_limit_exceeded` when the caller already holds the configured maximum
   * of executable non-terminal jobs (`admitting`/`queued`/`running` singles and
   * batch children; batch parents do not count). Idempotent replays of an
   * existing equivalent job are still admitted.
   *
   */
  429: ErrorResponse;
  /**
   * API admission dependency failure (for example OCR source object upload to dedicated
   * storage). Transient GPU/HPS unavailability is **not** reported as this response —
   * known registry models remain admissible and jobs stay durable/queued; tenant caps and
   * queue alarms bound/observe backlog.
   *
   */
  503: ErrorResponse;
  /**
   * Sync wait observed a durable job terminal status of `indeterminate` (for example an
   * ambiguous provider acceptance). Body is `ErrorResponse`. Clients should not assume the
   * operation can be safely retried without consulting job and billing state.
   *
   */
  504: ErrorResponse;
};

export type SuggestSchemaError = SuggestSchemaErrors[keyof SuggestSchemaErrors];

export type SuggestSchemaResponses = {
  /**
   * Suggested schema
   */
  200: SuggestSchemaResponse;
};

export type SuggestSchemaResponse2 = SuggestSchemaResponses[keyof SuggestSchemaResponses];

export type ListJobsData = {
  body?: never;
  path?: never;
  query?: {
    /**
     * Opaque pagination cursor from a prior `JobListResponse.next_cursor`. Jobs are
     * returned newest-first (`created_at` descending, `id` descending tie-break).
     *
     */
    cursor?: string;
    /**
     * Maximum number of job summaries to return.
     */
    limit?: number;
    /**
     * Optional filter by public job status. Admission-phase (`admitting`) jobs are
     * never listed.
     *
     */
    status?: JobStatus;
    /**
     * Optional filter by job operation.
     */
    operation?: JobOperation;
  };
  url: '/jobs';
};

export type ListJobsErrors = {
  /**
   * Malformed request
   */
  400: ErrorResponse;
  /**
   * Missing or invalid API key
   */
  401: ErrorResponse;
  /**
   * API key lacks required OCR scope (`*` or `ocr:full`)
   */
  403: ErrorResponse;
  /**
   * Rate or tenant concurrency limit exceeded. Tenant capacity uses
   * `tenant_limit_exceeded` when the caller already holds the configured maximum
   * of executable non-terminal jobs (`admitting`/`queued`/`running` singles and
   * batch children; batch parents do not count). Idempotent replays of an
   * existing equivalent job are still admitted.
   *
   */
  429: ErrorResponse;
};

export type ListJobsError = ListJobsErrors[keyof ListJobsErrors];

export type ListJobsResponses = {
  /**
   * Page of job summaries
   */
  200: JobListResponse;
};

export type ListJobsResponse = ListJobsResponses[keyof ListJobsResponses];

export type GetJobData = {
  body?: never;
  path: {
    /**
     * Durable OCR job identifier (`opj_…`).
     */
    id: JobId;
  };
  query?: {
    /**
     * Opaque pagination cursor for batch child summaries.
     */
    cursor?: string;
    /**
     * Maximum number of batch child summaries to return.
     */
    limit?: number;
  };
  url: '/jobs/{id}';
};

export type GetJobErrors = {
  /**
   * Missing or invalid API key
   */
  401: ErrorResponse;
  /**
   * API key lacks required OCR scope (`*` or `ocr:full`)
   */
  403: ErrorResponse;
  /**
   * Job not found for the authenticated tenant
   */
  404: ErrorResponse;
  /**
   * Rate or tenant concurrency limit exceeded. Tenant capacity uses
   * `tenant_limit_exceeded` when the caller already holds the configured maximum
   * of executable non-terminal jobs (`admitting`/`queued`/`running` singles and
   * batch children; batch parents do not count). Idempotent replays of an
   * existing equivalent job are still admitted.
   *
   */
  429: ErrorResponse;
};

export type GetJobError = GetJobErrors[keyof GetJobErrors];

export type GetJobResponses = {
  /**
   * Job status, progress, and result when available
   */
  200: Job;
};

export type GetJobResponse = GetJobResponses[keyof GetJobResponses];

export type GetJobResultData = {
  body?: never;
  path: {
    /**
     * Durable OCR job identifier (`opj_…`).
     */
    id: JobId;
  };
  query?: {
    /**
     * Parse representation to return. Defaults to the job's original `output_format`
     * when omitted. Fresh parses store both representations as immutable object-store
     * artifacts; historical jobs only expose the representation already present in
     * `result_json`. Requesting an unavailable alternate returns `422` and never
     * re-runs OCR.
     *
     */
    format?: OcrOutputFormat;
  };
  url: '/jobs/{id}/result';
};

export type GetJobResultErrors = {
  /**
   * Malformed request
   */
  400: ErrorResponse;
  /**
   * Missing or invalid API key
   */
  401: ErrorResponse;
  /**
   * API key lacks required OCR scope (`*` or `ocr:full`)
   */
  403: ErrorResponse;
  /**
   * Job not found for the authenticated tenant
   */
  404: ErrorResponse;
  /**
   * The parse job exists but is not yet terminal, so a representation body is not
   * available. Poll `GET /jobs/{id}` until `succeeded`, then retry this route.
   *
   */
  409: ErrorResponse;
  /**
   * The requested parse representation is not available without re-running OCR.
   * Typical cases: historical jobs that only stored the originally requested
   * `output_format`, non-parse jobs, or failed terminals. Clients should request
   * the available format (see `details.available_format`) or re-admit a new parse.
   *
   */
  422: ErrorResponse;
  /**
   * Rate or tenant concurrency limit exceeded. Tenant capacity uses
   * `tenant_limit_exceeded` when the caller already holds the configured maximum
   * of executable non-terminal jobs (`admitting`/`queued`/`running` singles and
   * batch children; batch parents do not count). Idempotent replays of an
   * existing equivalent job are still admitted.
   *
   */
  429: ErrorResponse;
  /**
   * Sync wait observed a durable job terminal status of `indeterminate` (for example an
   * ambiguous provider acceptance). Body is `ErrorResponse`. Clients should not assume the
   * operation can be safely retried without consulting job and billing state.
   *
   */
  504: ErrorResponse;
};

export type GetJobResultError = GetJobResultErrors[keyof GetJobResultErrors];

export type GetJobResultResponses = {
  /**
   * Selected parse representation (`ParsedDocument` or `RawParseResult`)
   */
  200: ParseResult;
};

export type GetJobResultResponse = GetJobResultResponses[keyof GetJobResultResponses];

export type GetJobSourceData = {
  body?: never;
  path: {
    /**
     * Durable OCR job identifier (`opj_…`).
     */
    id: JobId;
  };
  query?: never;
  url: '/jobs/{id}/source';
};

export type GetJobSourceErrors = {
  /**
   * Missing or invalid API key
   */
  401: ErrorResponse;
  /**
   * API key lacks required OCR scope (`*` or `ocr:full`)
   */
  403: ErrorResponse;
  /**
   * Job not found, or retained source bytes are unavailable for the authenticated tenant.
   * `job_not_found` covers unknown, malformed, and cross-tenant ids. `source_not_available`
   * covers batch parents, jobs without retained uploads, expired cleanup, and missing storage
   * objects.
   *
   */
  404: ErrorResponse;
  /**
   * Requested byte range is outside the retained source object.
   */
  416: unknown;
  /**
   * Rate or tenant concurrency limit exceeded. Tenant capacity uses
   * `tenant_limit_exceeded` when the caller already holds the configured maximum
   * of executable non-terminal jobs (`admitting`/`queued`/`running` singles and
   * batch children; batch parents do not count). Idempotent replays of an
   * existing equivalent job are still admitted.
   *
   */
  429: ErrorResponse;
};

export type GetJobSourceError = GetJobSourceErrors[keyof GetJobSourceErrors];

export type GetJobSourceResponses = {
  /**
   * Full retained source document
   */
  200: Blob | File;
  /**
   * Partial retained source bytes for a satisfiable `Range` request.
   */
  206: Blob | File;
};

export type GetJobSourceResponse = GetJobSourceResponses[keyof GetJobSourceResponses];

export type CreateFileData = {
  body: CreateFileUpload;
  path?: never;
  query?: never;
  url: '/files';
};

export type CreateFileErrors = {
  /**
   * Malformed request
   */
  400: ErrorResponse;
  /**
   * Missing or invalid API key
   */
  401: ErrorResponse;
  /**
   * API key lacks required OCR scope (`*` or `ocr:full`)
   */
  403: ErrorResponse;
  /**
   * Document or request limits exceeded
   */
  413: ErrorResponse;
  /**
   * Unsupported uploaded media type
   */
  415: ErrorResponse;
  /**
   * Rate or tenant concurrency limit exceeded. Tenant capacity uses
   * `tenant_limit_exceeded` when the caller already holds the configured maximum
   * of executable non-terminal jobs (`admitting`/`queued`/`running` singles and
   * batch children; batch parents do not count). Idempotent replays of an
   * existing equivalent job are still admitted.
   *
   */
  429: ErrorResponse;
};

export type CreateFileError = CreateFileErrors[keyof CreateFileErrors];

export type CreateFileResponses = {
  /**
   * Uploaded reusable file
   */
  200: PublicFile;
};

export type CreateFileResponse = CreateFileResponses[keyof CreateFileResponses];

export type DeleteFileData = {
  body?: never;
  path: {
    /**
     * Tenant-scoped reusable file identifier.
     */
    id: string;
  };
  query?: never;
  url: '/files/{id}';
};

export type DeleteFileErrors = {
  /**
   * Missing or invalid API key
   */
  401: ErrorResponse;
  /**
   * API key lacks required OCR scope (`*` or `ocr:full`)
   */
  403: ErrorResponse;
  /**
   * File not found (including cross-tenant identifiers)
   */
  404: ErrorResponse;
  /**
   * Rate or tenant concurrency limit exceeded. Tenant capacity uses
   * `tenant_limit_exceeded` when the caller already holds the configured maximum
   * of executable non-terminal jobs (`admitting`/`queued`/`running` singles and
   * batch children; batch parents do not count). Idempotent replays of an
   * existing equivalent job are still admitted.
   *
   */
  429: ErrorResponse;
};

export type DeleteFileError = DeleteFileErrors[keyof DeleteFileErrors];

export type DeleteFileResponses = {
  /**
   * File deleted
   */
  200: DeleteFileResponse;
};

export type DeleteFileResponse2 = DeleteFileResponses[keyof DeleteFileResponses];

export type GetFileData = {
  body?: never;
  path: {
    /**
     * Tenant-scoped reusable file identifier.
     */
    id: string;
  };
  query?: never;
  url: '/files/{id}';
};

export type GetFileErrors = {
  /**
   * Missing or invalid API key
   */
  401: ErrorResponse;
  /**
   * API key lacks required OCR scope (`*` or `ocr:full`)
   */
  403: ErrorResponse;
  /**
   * File not found (including cross-tenant identifiers)
   */
  404: ErrorResponse;
  /**
   * Rate or tenant concurrency limit exceeded. Tenant capacity uses
   * `tenant_limit_exceeded` when the caller already holds the configured maximum
   * of executable non-terminal jobs (`admitting`/`queued`/`running` singles and
   * batch children; batch parents do not count). Idempotent replays of an
   * existing equivalent job are still admitted.
   *
   */
  429: ErrorResponse;
};

export type GetFileError = GetFileErrors[keyof GetFileErrors];

export type GetFileResponses = {
  /**
   * Reusable file metadata
   */
  200: PublicFile;
};

export type GetFileResponse = GetFileResponses[keyof GetFileResponses];

export type GetFileContentData = {
  body?: never;
  path: {
    /**
     * Tenant-scoped reusable file identifier.
     */
    id: string;
  };
  query?: never;
  url: '/files/{id}/content';
};

export type GetFileContentErrors = {
  /**
   * Missing or invalid API key
   */
  401: ErrorResponse;
  /**
   * API key lacks required OCR scope (`*` or `ocr:full`)
   */
  403: ErrorResponse;
  /**
   * File not found (including cross-tenant identifiers)
   */
  404: ErrorResponse;
  /**
   * Rate or tenant concurrency limit exceeded. Tenant capacity uses
   * `tenant_limit_exceeded` when the caller already holds the configured maximum
   * of executable non-terminal jobs (`admitting`/`queued`/`running` singles and
   * batch children; batch parents do not count). Idempotent replays of an
   * existing equivalent job are still admitted.
   *
   */
  429: ErrorResponse;
};

export type GetFileContentError = GetFileContentErrors[keyof GetFileContentErrors];

export type GetFileContentResponses = {
  /**
   * File content
   */
  200: Blob | File;
};

export type GetFileContentResponse = GetFileContentResponses[keyof GetFileContentResponses];

export type ListExtractionPipelinesData = {
  body?: never;
  path?: never;
  query?: never;
  url: '/pipelines';
};

export type ListExtractionPipelinesErrors = {
  /**
   * Missing or invalid API key
   */
  401: ErrorResponse;
  /**
   * API key lacks required OCR scope (`*` or `ocr:full`)
   */
  403: ErrorResponse;
  /**
   * Rate or tenant concurrency limit exceeded. Tenant capacity uses
   * `tenant_limit_exceeded` when the caller already holds the configured maximum
   * of executable non-terminal jobs (`admitting`/`queued`/`running` singles and
   * batch children; batch parents do not count). Idempotent replays of an
   * existing equivalent job are still admitted.
   *
   */
  429: ErrorResponse;
};

export type ListExtractionPipelinesError =
  ListExtractionPipelinesErrors[keyof ListExtractionPipelinesErrors];

export type ListExtractionPipelinesResponses = {
  /**
   * Successful response
   */
  200: ExtractionPipelineListResponse;
};

export type ListExtractionPipelinesResponse =
  ListExtractionPipelinesResponses[keyof ListExtractionPipelinesResponses];

export type CreateExtractionPipelineData = {
  body: CreateExtractionPipelineRequest2;
  path?: never;
  query?: never;
  url: '/pipelines';
};

export type CreateExtractionPipelineErrors = {
  /**
   * Malformed request
   */
  400: ErrorResponse;
  /**
   * Missing or invalid API key
   */
  401: ErrorResponse;
  /**
   * API key lacks required OCR scope (`*` or `ocr:full`)
   */
  403: ErrorResponse;
  /**
   * Pipeline name or slug already exists for the authenticated tenant
   */
  409: ErrorResponse;
  /**
   * Invalid schema, configuration, encrypted PDF, unsupported OCR model, or unsupported LLM model
   */
  422: ErrorResponse;
  /**
   * Rate or tenant concurrency limit exceeded. Tenant capacity uses
   * `tenant_limit_exceeded` when the caller already holds the configured maximum
   * of executable non-terminal jobs (`admitting`/`queued`/`running` singles and
   * batch children; batch parents do not count). Idempotent replays of an
   * existing equivalent job are still admitted.
   *
   */
  429: ErrorResponse;
};

export type CreateExtractionPipelineError =
  CreateExtractionPipelineErrors[keyof CreateExtractionPipelineErrors];

export type CreateExtractionPipelineResponses = {
  /**
   * Successful response
   */
  200: ExtractionPipeline;
};

export type CreateExtractionPipelineResponse =
  CreateExtractionPipelineResponses[keyof CreateExtractionPipelineResponses];

export type DeleteExtractionPipelineData = {
  body?: never;
  path: {
    /**
     * Tenant-scoped saved extraction pipeline identifier (`oppl_…`).
     */
    id: ExtractionPipelineId;
  };
  query?: never;
  url: '/pipelines/{id}';
};

export type DeleteExtractionPipelineErrors = {
  /**
   * Missing or invalid API key
   */
  401: ErrorResponse;
  /**
   * API key lacks required OCR scope (`*` or `ocr:full`)
   */
  403: ErrorResponse;
  /**
   * Pipeline not found for the authenticated tenant (also used for deleted or cross-tenant ids)
   */
  404: ErrorResponse;
  /**
   * Rate or tenant concurrency limit exceeded. Tenant capacity uses
   * `tenant_limit_exceeded` when the caller already holds the configured maximum
   * of executable non-terminal jobs (`admitting`/`queued`/`running` singles and
   * batch children; batch parents do not count). Idempotent replays of an
   * existing equivalent job are still admitted.
   *
   */
  429: ErrorResponse;
};

export type DeleteExtractionPipelineError =
  DeleteExtractionPipelineErrors[keyof DeleteExtractionPipelineErrors];

export type DeleteExtractionPipelineResponses = {
  /**
   * Successful response
   */
  200: DeleteExtractionPipelineResponse;
};

export type DeleteExtractionPipelineResponse2 =
  DeleteExtractionPipelineResponses[keyof DeleteExtractionPipelineResponses];

export type GetExtractionPipelineData = {
  body?: never;
  path: {
    /**
     * Tenant-scoped saved extraction pipeline identifier (`oppl_…`).
     */
    id: ExtractionPipelineId;
  };
  query?: never;
  url: '/pipelines/{id}';
};

export type GetExtractionPipelineErrors = {
  /**
   * Missing or invalid API key
   */
  401: ErrorResponse;
  /**
   * API key lacks required OCR scope (`*` or `ocr:full`)
   */
  403: ErrorResponse;
  /**
   * Pipeline not found for the authenticated tenant (also used for deleted or cross-tenant ids)
   */
  404: ErrorResponse;
  /**
   * Rate or tenant concurrency limit exceeded. Tenant capacity uses
   * `tenant_limit_exceeded` when the caller already holds the configured maximum
   * of executable non-terminal jobs (`admitting`/`queued`/`running` singles and
   * batch children; batch parents do not count). Idempotent replays of an
   * existing equivalent job are still admitted.
   *
   */
  429: ErrorResponse;
};

export type GetExtractionPipelineError =
  GetExtractionPipelineErrors[keyof GetExtractionPipelineErrors];

export type GetExtractionPipelineResponses = {
  /**
   * Successful response
   */
  200: ExtractionPipeline;
};

export type GetExtractionPipelineResponse =
  GetExtractionPipelineResponses[keyof GetExtractionPipelineResponses];

export type UpdateExtractionPipelineData = {
  body: UpdateExtractionPipelineRequest2;
  path: {
    /**
     * Tenant-scoped saved extraction pipeline identifier (`oppl_…`).
     */
    id: ExtractionPipelineId;
  };
  query?: never;
  url: '/pipelines/{id}';
};

export type UpdateExtractionPipelineErrors = {
  /**
   * Malformed request
   */
  400: ErrorResponse;
  /**
   * Missing or invalid API key
   */
  401: ErrorResponse;
  /**
   * API key lacks required OCR scope (`*` or `ocr:full`)
   */
  403: ErrorResponse;
  /**
   * Pipeline not found for the authenticated tenant (also used for deleted or cross-tenant ids)
   */
  404: ErrorResponse;
  /**
   * Pipeline name or slug already exists for the authenticated tenant
   */
  409: ErrorResponse;
  /**
   * Invalid schema, configuration, encrypted PDF, unsupported OCR model, or unsupported LLM model
   */
  422: ErrorResponse;
  /**
   * Rate or tenant concurrency limit exceeded. Tenant capacity uses
   * `tenant_limit_exceeded` when the caller already holds the configured maximum
   * of executable non-terminal jobs (`admitting`/`queued`/`running` singles and
   * batch children; batch parents do not count). Idempotent replays of an
   * existing equivalent job are still admitted.
   *
   */
  429: ErrorResponse;
};

export type UpdateExtractionPipelineError =
  UpdateExtractionPipelineErrors[keyof UpdateExtractionPipelineErrors];

export type UpdateExtractionPipelineResponses = {
  /**
   * Successful response
   */
  200: ExtractionPipeline;
};

export type UpdateExtractionPipelineResponse =
  UpdateExtractionPipelineResponses[keyof UpdateExtractionPipelineResponses];
