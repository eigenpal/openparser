// This file is auto-generated by @hey-api/openapi-ts

import {
  type Client,
  formDataBodySerializer,
  type Options as Options2,
  type TDataShape,
} from './client';
import { client } from './client.gen';
import type {
  CreateExtractionPipelineData,
  CreateExtractionPipelineErrors,
  CreateExtractionPipelineResponses,
  CreateFileData,
  CreateFileErrors,
  CreateFileResponses,
  DeleteExtractionPipelineData,
  DeleteExtractionPipelineErrors,
  DeleteExtractionPipelineResponses,
  DeleteFileData,
  DeleteFileErrors,
  DeleteFileResponses,
  ExtractAsyncData,
  ExtractAsyncErrors,
  ExtractAsyncResponses,
  ExtractBatchData,
  ExtractBatchErrors,
  ExtractBatchResponses,
  ExtractSyncData,
  ExtractSyncErrors,
  ExtractSyncResponses,
  GetExtractionPipelineData,
  GetExtractionPipelineErrors,
  GetExtractionPipelineResponses,
  GetFileContentData,
  GetFileContentErrors,
  GetFileContentResponses,
  GetFileData,
  GetFileErrors,
  GetFileResponses,
  GetJobData,
  GetJobErrors,
  GetJobResponses,
  GetJobResultData,
  GetJobResultErrors,
  GetJobResultResponses,
  GetJobSourceData,
  GetJobSourceErrors,
  GetJobSourceResponses,
  ListExtractionPipelinesData,
  ListExtractionPipelinesErrors,
  ListExtractionPipelinesResponses,
  ListJobsData,
  ListJobsErrors,
  ListJobsResponses,
  ListLlmModelsData,
  ListLlmModelsErrors,
  ListLlmModelsResponses,
  ListOcrModelsData,
  ListOcrModelsErrors,
  ListOcrModelsResponses,
  ParseAsyncData,
  ParseAsyncErrors,
  ParseAsyncResponses,
  ParseBatchData,
  ParseBatchErrors,
  ParseBatchResponses,
  ParseSyncData,
  ParseSyncErrors,
  ParseSyncResponses,
  SuggestSchemaData,
  SuggestSchemaErrors,
  SuggestSchemaResponses,
  UpdateExtractionPipelineData,
  UpdateExtractionPipelineErrors,
  UpdateExtractionPipelineResponses,
} from './types.gen';

export type Options<
  TData extends TDataShape = TDataShape,
  ThrowOnError extends boolean = boolean,
  TResponse = unknown,
> = Options2<TData, ThrowOnError, TResponse> & {
  /**
   * You can provide a client instance returned by `createClient()` instead of
   * individual options. This might be also useful if you want to implement a
   * custom client.
   */
  client?: Client;
  /**
   * You can pass arbitrary values through the `meta` object. This can be
   * used to access values that aren't defined as part of the SDK function.
   */
  meta?: Record<string, unknown>;
};

/**
 * List OCR models
 *
 * Returns the hosted OCR model registry with current capabilities, option defaults,
 * retail page prices, and availability. Clients should discover this endpoint instead of
 * hard-coding model IDs or provider-specific options.
 *
 */
export const listOcrModels = <ThrowOnError extends boolean = false>(
  options?: Options<ListOcrModelsData, ThrowOnError>
) =>
  (options?.client ?? client).get<ListOcrModelsResponses, ListOcrModelsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/models/ocr',
    ...options,
  });

/**
 * List compatible LLM models
 *
 * Returns a cached, normalized OpenRouter catalog filtered to models that advertise
 * strict structured output (`structured_outputs` + `response_format`), have valid finite
 * provider-list cost (internally) that is converted to customer retail rates, produce text, and are not expired.
 *
 * Default `mode=suggested` returns a short recommended subset. `mode=search` discovers the
 * full compatible catalog with optional `q` / pagination. Field grounding and schema
 * suggestion remain restricted to certified models.
 *
 */
export const listLlmModels = <ThrowOnError extends boolean = false>(
  options?: Options<ListLlmModelsData, ThrowOnError>
) =>
  (options?.client ?? client).get<ListLlmModelsResponses, ListLlmModelsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/models/llm',
    ...options,
  });

/**
 * Parse a document synchronously
 *
 * Admit a parse job, wait up to the sync wait limit, and return the selected terminal parse
 * representation when ready. `output_format` defaults to the versioned provider-neutral
 * `openparser@1`; `raw` returns a stable provider envelope around the selected provider's
 * untouched successful result. Terminal `failed` within the wait window returns `422`
 * `ErrorResponse`; terminal `indeterminate` returns `504` `ErrorResponse`. Returns `202`
 * with a durable job reference and `Location` if the wait limit expires first.
 *
 * Example (`multipart/form-data`):
 *
 * ```bash
 * curl -X POST 'https://api.openparser.dev/parse' \
 * -H 'Authorization: Bearer YOUR_API_KEY' \
 * -H "Idempotency-Key: $(uuidgen 2>/dev/null || openssl rand -hex 16)" \
 * -F 'request={"ocr_model":"paddleocr-vl-1.6","output_format":"openparser@1"};type=application/json' \
 * -F 'file=@./document.pdf'
 * ```
 *
 */
export const parseSync = <ThrowOnError extends boolean = false>(
  options: Options<ParseSyncData, ThrowOnError>
) =>
  (options.client ?? client).post<ParseSyncResponses, ParseSyncErrors, ThrowOnError>({
    ...formDataBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/parse',
    ...options,
    headers: {
      'Content-Type': null,
      ...options.headers,
    },
  });

/**
 * Parse a document asynchronously
 *
 * Admit a durable parse job and return immediately.
 *
 * Example (`multipart/form-data`):
 *
 * ```bash
 * curl -X POST 'https://api.openparser.dev/parse/async' \
 * -H 'Authorization: Bearer YOUR_API_KEY' \
 * -H "Idempotency-Key: $(uuidgen 2>/dev/null || openssl rand -hex 16)" \
 * -F 'request={"ocr_model":"paddleocr-vl-1.6","output_format":"openparser@1"};type=application/json' \
 * -F 'file=@./document.pdf'
 * ```
 *
 */
export const parseAsync = <ThrowOnError extends boolean = false>(
  options: Options<ParseAsyncData, ThrowOnError>
) =>
  (options.client ?? client).post<ParseAsyncResponses, ParseAsyncErrors, ThrowOnError>({
    ...formDataBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/parse/async',
    ...options,
    headers: {
      'Content-Type': null,
      ...options.headers,
    },
  });

/**
 * Parse multiple documents in one batch
 *
 * Admit up to 100 parse child jobs in one durable batch. Every uploaded file must be mapped
 * exactly once by `file_index`. Partial child failures are preserved in batch results.
 * Parent status derivation for mixed outcomes is defined on `JobStatus`.
 *
 */
export const parseBatch = <ThrowOnError extends boolean = false>(
  options: Options<ParseBatchData, ThrowOnError>
) =>
  (options.client ?? client).post<ParseBatchResponses, ParseBatchErrors, ThrowOnError>({
    ...formDataBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/parse/batch',
    ...options,
    headers: {
      'Content-Type': null,
      ...options.headers,
    },
  });

/**
 * Extract structured data synchronously
 *
 * Admit a single extract job from file bytes, `file_id`, or a tenant-owned succeeded
 * `parse_job_id`, then run schema-constrained OpenRouter extraction via `llm_model`. A
 * `parse_job_id` source reuses the canonical ParsedDocument (from dual artifacts or legacy
 * `result_json`) without OCR or another page charge; historical raw-only parses return
 * `422 canonical_result_unavailable`. Wait up to the sync wait limit and return
 * `ExtractionTerminalResult` when ready. Terminal `failed` within the wait window returns
 * `422`; terminal `indeterminate` returns `504`. Returns `202` with a durable job reference
 * and `Location` if the wait expires.
 *
 */
export const extractSync = <ThrowOnError extends boolean = false>(
  options: Options<ExtractSyncData, ThrowOnError>
) =>
  (options.client ?? client).post<ExtractSyncResponses, ExtractSyncErrors, ThrowOnError>({
    ...formDataBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/extract',
    ...options,
    headers: {
      'Content-Type': null,
      ...options.headers,
    },
  });

/**
 * Extract structured data asynchronously
 *
 * Admit a durable single extract job from file bytes, `file_id`, or `parse_job_id` and
 * return immediately. Parse reuse does not run OCR or add a page charge; OpenRouter usage
 * remains billable.
 *
 */
export const extractAsync = <ThrowOnError extends boolean = false>(
  options: Options<ExtractAsyncData, ThrowOnError>
) =>
  (options.client ?? client).post<ExtractAsyncResponses, ExtractAsyncErrors, ThrowOnError>({
    ...formDataBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/extract/async',
    ...options,
    headers: {
      'Content-Type': null,
      ...options.headers,
    },
  });

/**
 * Extract structured data from multiple documents
 *
 * Admit up to 100 file-backed extract child jobs in one durable batch. Every item uses
 * `file_index` or `file_id`; `parse_job_id` reuse is single-extract-only and is not accepted
 * in batch. Partial child failures are preserved. Parent status derivation follows
 * `JobStatus`; billing is evaluated per child.
 *
 */
export const extractBatch = <ThrowOnError extends boolean = false>(
  options: Options<ExtractBatchData, ThrowOnError>
) =>
  (options.client ?? client).post<ExtractBatchResponses, ExtractBatchErrors, ThrowOnError>({
    ...formDataBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/extract/batch',
    ...options,
    headers: {
      'Content-Type': null,
      ...options.headers,
    },
  });

/**
 * Suggest an extraction schema
 *
 * Suggest an extraction JSON Schema from a tenant-owned succeeded parse result.
 * Optional `hint` is capped at 500 characters. Definitive failures return
 * `schema_suggestion_failed`.
 *
 */
export const suggestSchema = <ThrowOnError extends boolean = false>(
  options: Options<SuggestSchemaData, ThrowOnError>
) =>
  (options.client ?? client).post<SuggestSchemaResponses, SuggestSchemaErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/suggest-schema',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * List jobs
 *
 * Tenant-scoped chronological list of top-level OCR jobs (batch children are omitted).
 * Returns lightweight summaries only — full results remain on `GET /jobs/{id}`.
 * Cursor pagination is stable newest-first with an `id` tie-break. Optional `status`
 * and `operation` filters are validated strictly; unknown values return `400`.
 *
 */
export const listJobs = <ThrowOnError extends boolean = false>(
  options?: Options<ListJobsData, ThrowOnError>
) =>
  (options?.client ?? client).get<ListJobsResponses, ListJobsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/jobs',
    ...options,
  });

/**
 * Get job status and result
 *
 * Tenant-scoped durable job read. Jobs belonging to another tenant return `404`. Batch jobs
 * include cursor-paginated child summaries. Playground tokens (`ocr:playground`) and `ocr:full`
 * keys are both accepted. Successful parse jobs return the originally requested `output_format`
 * in `result`; use `GET /jobs/{id}/result?format=` to select an alternate stored representation
 * without re-running OCR.
 *
 */
export const getJob = <ThrowOnError extends boolean = false>(
  options: Options<GetJobData, ThrowOnError>
) =>
  (options.client ?? client).get<GetJobResponses, GetJobErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/jobs/{id}',
    ...options,
  });

/**
 * Get a parse job result representation
 *
 * Return the selected parse wire body for a tenant-owned succeeded parse job.
 * `format` defaults to the job's original `output_format`. Fresh dual-artifact parses
 * can serve either `openparser@1` or `raw` from object storage without re-running HPS
 * or charging pages. Historical jobs only serve the representation already present in
 * `result_json`; unavailable alternates return `422 result_format_unavailable` with
 * guidance. Non-terminal jobs return `409 job_not_terminal`. Playground tokens
 * (`ocr:playground`) and `ocr:full` keys are both accepted.
 *
 */
export const getJobResult = <ThrowOnError extends boolean = false>(
  options: Options<GetJobResultData, ThrowOnError>
) =>
  (options.client ?? client).get<GetJobResultResponses, GetJobResultErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/jobs/{id}/result',
    ...options,
  });

/**
 * Download retained job source bytes
 *
 * Stream the retained source document for a parse or single-document extract job. Jobs without
 * retained source bytes return `404 source_not_available`. Check `has_source` on `GET /jobs/{id}`
 * before calling. Playground tokens (`ocr:playground`) and `ocr:full` keys are both accepted,
 * matching other job read routes. Optional `Range` requests may return `206` partial content or
 * `416` when the range is unsatisfiable.
 *
 */
export const getJobSource = <ThrowOnError extends boolean = false>(
  options: Options<GetJobSourceData, ThrowOnError>
) =>
  (options.client ?? client).get<GetJobSourceResponses, GetJobSourceErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/jobs/{id}/source',
    ...options,
  });

/**
 * Upload a reusable file
 *
 * Upload bytes directly to OpenParser's Eigenpal-compatible tenant file pool. Files may be
 * reused by later parse or extract requests through `file_id`; bytes do not pass through
 * Vercel.
 *
 */
export const createFile = <ThrowOnError extends boolean = false>(
  options: Options<CreateFileData, ThrowOnError>
) =>
  (options.client ?? client).post<CreateFileResponses, CreateFileErrors, ThrowOnError>({
    ...formDataBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/files',
    ...options,
    headers: {
      'Content-Type': null,
      ...options.headers,
    },
  });

/**
 * Delete a reusable file
 */
export const deleteFile = <ThrowOnError extends boolean = false>(
  options: Options<DeleteFileData, ThrowOnError>
) =>
  (options.client ?? client).delete<DeleteFileResponses, DeleteFileErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/files/{id}',
    ...options,
  });

/**
 * Get reusable file metadata
 */
export const getFile = <ThrowOnError extends boolean = false>(
  options: Options<GetFileData, ThrowOnError>
) =>
  (options.client ?? client).get<GetFileResponses, GetFileErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/files/{id}',
    ...options,
  });

/**
 * Download reusable file bytes
 */
export const getFileContent = <ThrowOnError extends boolean = false>(
  options: Options<GetFileContentData, ThrowOnError>
) =>
  (options.client ?? client).get<GetFileContentResponses, GetFileContentErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/files/{id}/content',
    ...options,
  });

/**
 * List saved extraction pipelines
 *
 * Return active (non-deleted) pipelines for the authenticated tenant, newest updates first.
 *
 */
export const listExtractionPipelines = <ThrowOnError extends boolean = false>(
  options?: Options<ListExtractionPipelinesData, ThrowOnError>
) =>
  (options?.client ?? client).get<
    ListExtractionPipelinesResponses,
    ListExtractionPipelinesErrors,
    ThrowOnError
  >({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/pipelines',
    ...options,
  });

/**
 * Create a saved extraction pipeline
 *
 * Persist a tenant-scoped extraction configuration (OCR model, LLM model, schema,
 * repair/grounding/reasoning). Validated with the same admission gates as extract.
 * Playground tokens and `ocr:full` keys are both accepted.
 *
 */
export const createExtractionPipeline = <ThrowOnError extends boolean = false>(
  options: Options<CreateExtractionPipelineData, ThrowOnError>
) =>
  (options.client ?? client).post<
    CreateExtractionPipelineResponses,
    CreateExtractionPipelineErrors,
    ThrowOnError
  >({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/pipelines',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * Soft-delete a saved extraction pipeline
 *
 * Marks the pipeline deleted. Subsequent extract admits with that id return
 * `404 pipeline_not_found`. Historical jobs retain snapshotted config.
 *
 */
export const deleteExtractionPipeline = <ThrowOnError extends boolean = false>(
  options: Options<DeleteExtractionPipelineData, ThrowOnError>
) =>
  (options.client ?? client).delete<
    DeleteExtractionPipelineResponses,
    DeleteExtractionPipelineErrors,
    ThrowOnError
  >({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/pipelines/{id}',
    ...options,
  });

/**
 * Get a saved extraction pipeline
 *
 * Return one active pipeline. Missing, deleted, and cross-tenant ids return
 * `404 pipeline_not_found`.
 *
 */
export const getExtractionPipeline = <ThrowOnError extends boolean = false>(
  options: Options<GetExtractionPipelineData, ThrowOnError>
) =>
  (options.client ?? client).get<
    GetExtractionPipelineResponses,
    GetExtractionPipelineErrors,
    ThrowOnError
  >({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/pipelines/{id}',
    ...options,
  });

/**
 * Update a saved extraction pipeline
 *
 * Patch one or more fields, re-validate the effective merged config, and atomically
 * increment `version`. In-flight and completed jobs keep their admit-time snapshots.
 *
 */
export const updateExtractionPipeline = <ThrowOnError extends boolean = false>(
  options: Options<UpdateExtractionPipelineData, ThrowOnError>
) =>
  (options.client ?? client).patch<
    UpdateExtractionPipelineResponses,
    UpdateExtractionPipelineErrors,
    ThrowOnError
  >({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/pipelines/{id}',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });
